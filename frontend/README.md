# Frontend

React single-page app (Vite + TypeScript). Runs on `http://localhost:5173`.

Will contain:
- `src/` — pages, components, API client, auth state
- `package.json` — dependencies (installed with `npm install`)
